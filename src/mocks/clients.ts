import type { Client } from '../types';

/** Populated at runtime — Property Seeker visit requests create leads here (see visitService), consultants set them up (see clientService). */
export const clients: Client[] = [];
