/** Super Admin is the platform operator — not created through any UI, so its credential is fixed. */
export const SUPER_ADMIN_CREDENTIALS = {
  email: 'superadmin@realportal.ph',
  password: 'SuperAdmin123',
};
